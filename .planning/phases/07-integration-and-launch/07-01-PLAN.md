---
phase: 07-integration-and-launch
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/seed-elken.mjs
  - supabase/elken-seed.sql
autonomous: true
requirements:
  - SEED-01
  - SEED-02
  - SEED-03
  - SEED-04

must_haves:
  truths:
    - "Running `node scripts/seed-elken.mjs` creates the Elken tenant, Ask Ethan Digital bot, all GenQi FAQs in EN/BM/ZH, all response templates, and personality config with no manual steps"
    - "Re-running the seed script is safe and idempotent — no duplicate FAQs, templates are updated, bot name and personality are always written correctly"
    - "The SQL seed file supabase/elken-seed.sql no longer exists in the repository"
  artifacts:
    - path: "scripts/seed-elken.mjs"
      provides: "Complete Elken seed script with tenant, bot, GenQi FAQs, response templates, personality config"
      contains: "Ask Ethan Digital"
  key_links:
    - from: "scripts/seed-elken.mjs"
      to: "Supabase bots table"
      via: "supabase.from('bots').update()"
      pattern: "greeting_en.*greeting_bm.*greeting_zh"
    - from: "scripts/seed-elken.mjs"
      to: "Supabase faqs table"
      via: "supabase.from('faqs').insert()"
      pattern: "genqigex@gmail.com"
    - from: "scripts/seed-elken.mjs"
      to: "Supabase response_templates table"
      via: "supabase.from('response_templates').upsert()"
      pattern: "slot_full.*booking_confirmed_member.*reminder_24h.*post_survey.*no_product_found"
---

<objective>
Complete the Elken seed script with all GenQi FAQ content, client-provided response templates, and personality configuration; delete the redundant SQL seed file.

Purpose: SEED-01 through SEED-04 require a single `node scripts/seed-elken.mjs` command to fully provision the Elken bot with all production data. The existing partial seed has the wrong bot name, missing GenQi FAQs, missing client templates, and no personality config.

Output: A production-ready idempotent seed script and removal of the obsolete SQL seed.
</objective>

<execution_context>
@/Users/abiramivasudevan/.claude/get-shit-done/workflows/execute-plan.md
@/Users/abiramivasudevan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-integration-and-launch/07-CONTEXT.md
@.planning/phases/07-integration-and-launch/07-RESEARCH.md

@scripts/seed-elken.mjs
@supabase/elken-seed.sql
</context>

<tasks>

<task type="auto">
  <name>Task 1: Complete seed-elken.mjs with full Elken data and personality config</name>
  <files>scripts/seed-elken.mjs</files>
  <read_first>
    scripts/seed-elken.mjs
    .planning/phases/07-integration-and-launch/07-CONTEXT.md
    .planning/phases/07-integration-and-launch/07-RESEARCH.md
  </read_first>
  <action>
Extend `scripts/seed-elken.mjs` in-place. Keep the existing .env.local parsing, Supabase client setup, and fixed UUIDs (`TENANT_ID = '24284717-f4c4-4e26-9174-beeca562f616'`, `BOT_ID = '6176aa27-ce33-4dbc-b478-407414f86cac'`). Make these changes:

**1. Bot upsert + forced update (SEED-01, SEED-04):**

Keep the existing tenant upsert (unchanged). For the bot:
- Keep the upsert with `ignoreDuplicates: true` for row creation
- AFTER the upsert, add an unconditional `.update()` call on the bot row by `id` to always write:
  - `name: 'Ask Ethan Digital'` (was "Ethan" — per D-01)
  - `greeting_en` — exact text from CONTEXT.md (use backtick template literal to preserve newlines and emoji):
    ```
    Hi! 😊 Thank you for contacting Elken. My name is Ethan, I'll be your assistant for today — what can I do for you?\n1. Product enquiries\n2. GenQi facilities Booking
    ```
  - `greeting_bm` — exact text:
    ```
    Hai! 😊 Terima kasih kerana menghubungi Elken. Nama saya Ethan, saya akan menjadi pembantu anda untuk hari ini — apakah yang boleh saya bantu?\n1. Pertanyaan Produk\n2. Tempahan Kemudahan GenQi
    ```
  - `greeting_zh` — exact text:
    ```
    您好！😊 感谢您联系 Elken。我是 Ethan，很高兴为您服务 — 请问今天有什么可以帮到您？\n1. 一般咨询\n2. GenQi 设施预订
    ```
  - `tone: 'Friendly'` (capital F — DB CHECK constraint requires title case)
  - `feature_flags: { rag: true, multilingual: true, booking_enabled: true }` (per D-03)

Log: `console.log('✓ Bot: Ask Ethan Digital (personality configured)')`

**2. FAQ count-guard + GenQi FAQs (SEED-02):**

Replace the existing FAQ insert with the count-guard pattern:
```javascript
const { count: faqCount } = await supabase
  .from('faqs')
  .select('id', { count: 'exact', head: true })
  .eq('bot_id', BOT_ID)

if (faqCount === 0) {
  const { error: faqErr } = await supabase.from('faqs').insert(faqs)
  if (faqErr) throw new Error(`FAQs: ${faqErr.message}`)
  console.log(`✓ FAQs: ${faqs.length} rows (en/bm/zh)`)
} else {
  console.log(`✓ FAQs: ${faqCount} rows already present — skipping`)
}
```

Keep the existing 15 general Elken FAQs (5 questions x 3 languages). ADD the following GenQi FAQs in EN, BM, and ZH. Use these exact factual details (translate prose framing naturally, keep all facts verbatim):

**GenQi OKR FAQs (EN/BM/ZH for each):**
- Q: "What are GenQi Old Klang Road's operating hours and contact details?" / A: Include email genqigex@gmail.com, phone 012-2208396, hours Mon-Sun 10am-10pm, public holidays open except CNY/Raya/Deepavali/Christmas with advance notice
- Q: "What bed facilities are available at GenQi Old Klang Road?" / A: Female beds 5 units 1.5hr/session last booking 8pm BES on loan 5 units; Male beds 2 units 1.5hr/session last booking 8pm
- Q: "What is the Inhaler facility at GenQi Old Klang Road?" / A: 8 chairs, 30min or 1hr session, last booking 8:30pm (1hr) / 9pm (30min)
- Q: "What meeting rooms are available at GenQi Old Klang Road?" / A: Small max 8 pax TV+projector; Large max 50 pax TV+projector+table seating; Elken members only with valid membership ID

**GenQi Subang FAQs (EN/BM/ZH for each):**
- Q: "What are GenQi Subang's operating hours and contact details?" / A: Include email genqics@gmail.com, phone 0122206215, hours Weekdays 10am-6:30pm, weekends and public holidays closed
- Q: "What bed facilities are available at GenQi Subang?" / A: Female beds 5 units 1.5hr/session last booking 4:45pm BES on loan 4 units; Male beds 2 units 1.5hr/session last booking 4:45pm; Unisex beds 2 units one gender at a time only no mixing
- Q: "What is the Inhaler facility at GenQi Subang?" / A: 5 chairs, 30min or 1hr session, last booking 3:15pm (1hr) / 3:45pm (30min); No meeting rooms at Subang

**General Rules FAQs (EN/BM/ZH for each):**
- Q: "What are the general rules for visiting GenQi?" / A: No food or drinks on premises; arrive 15 minutes early for registration
- Q: "What is the booking process for Elken members vs non-members?" / A: Members booking confirmed directly, ask about BES device for bed bookings; Non-members specialist contacts within 24 hours

That gives ~9 GenQi FAQ questions x 3 languages = 27 GenQi rows + 15 existing = 42 total FAQ rows.

Each FAQ object: `{ bot_id: BOT_ID, question: '...', answer: '...', language: 'en'|'bm'|'zh' }`

**3. Response templates update (SEED-03):**

Replace existing templates array with ALL templates. Use `ignoreDuplicates: false` so existing draft content is overwritten with client-provided verbatim text.

Keep the 5 existing intent_keys (browse_product, health_issue, book_session, faq, general). ADD these new intent_keys with the EXACT verbatim text from CONTEXT.md (keep template placeholders like `<name>`, `<time>`, `<facility>`, `<date & time>`, `<with BES / no BES>` as literal strings):

- `slot_full` (EN/BM/ZH) — exact text from CONTEXT.md
- `booking_confirmed_member` (EN/BM/ZH) — exact text from CONTEXT.md
- `booking_confirmed_nonmember` (EN/BM/ZH) — exact text from CONTEXT.md
- `reminder_24h` (EN/BM/ZH) — exact text from CONTEXT.md
- `post_survey` (EN/BM/ZH) — exact text from CONTEXT.md
- `no_product_found` (EN/BM/ZH) — exact text from CONTEXT.md

Total: 11 intent_keys x 3 languages = 33 template rows.

Change the upsert to: `{ onConflict: 'bot_id,intent_key,language', ignoreDuplicates: false }`

Update the console.log to: `console.log('✓ Response templates: ${templates.length} rows (11 intents x en/bm/zh)')`

**4. Summary output:**

Update the final output to:
```javascript
console.log('\nSeed complete!')
console.log(`  Tenant ID: ${TENANT_ID}`)
console.log(`  Bot ID:    ${BOT_ID}`)
console.log(`  Bot name:  Ask Ethan Digital`)
console.log(`  FAQs:      ${faqs.length} rows`)
console.log(`  Templates: ${templates.length} rows`)
```
  </action>
  <verify>
    <automated>node -c "scripts/seed-elken.mjs" 2>&1 || echo "Syntax check not applicable to .mjs"; grep -c "Ask Ethan Digital" scripts/seed-elken.mjs && grep -c "genqigex@gmail.com" scripts/seed-elken.mjs && grep -c "slot_full" scripts/seed-elken.mjs && grep -c "booking_confirmed_member" scripts/seed-elken.mjs && grep -c "no_product_found" scripts/seed-elken.mjs && grep -c "ignoreDuplicates: false" scripts/seed-elken.mjs && grep -c "greeting_en" scripts/seed-elken.mjs && grep "booking_enabled" scripts/seed-elken.mjs | head -1</automated>
  </verify>
  <acceptance_criteria>
    - scripts/seed-elken.mjs contains the string `Ask Ethan Digital` (at least 3 occurrences: upsert, update, console.log)
    - scripts/seed-elken.mjs contains `greeting_en` and `greeting_bm` and `greeting_zh` in the bot update call
    - scripts/seed-elken.mjs contains `tone: 'Friendly'` (capital F)
    - scripts/seed-elken.mjs contains `booking_enabled: true` inside feature_flags
    - scripts/seed-elken.mjs contains `genqigex@gmail.com` (OKR contact in FAQ)
    - scripts/seed-elken.mjs contains `genqics@gmail.com` (Subang contact in FAQ)
    - scripts/seed-elken.mjs contains `0122206215` (Subang phone in FAQ)
    - scripts/seed-elken.mjs contains all 6 new intent_keys: `slot_full`, `booking_confirmed_member`, `booking_confirmed_nonmember`, `reminder_24h`, `post_survey`, `no_product_found`
    - scripts/seed-elken.mjs uses `ignoreDuplicates: false` for the template upsert
    - scripts/seed-elken.mjs uses the FAQ count-guard pattern (`count: 'exact'` before insert)
    - scripts/seed-elken.mjs contains `.update(` call after the bot upsert (forced personality update)
    - `npx vitest run` exits 0 (no regressions)
  </acceptance_criteria>
  <done>Seed script contains all Elken production data: correct bot name, personality config with greetings in 3 languages, ~42 FAQs covering GenQi OKR + Subang + general rules + existing member FAQs, 33 response templates covering 11 intents in 3 languages. Idempotent via count-guard for FAQs and forced update for bot row.</done>
</task>

<task type="auto">
  <name>Task 2: Delete redundant SQL seed file</name>
  <files>supabase/elken-seed.sql</files>
  <read_first>
    supabase/elken-seed.sql
  </read_first>
  <action>
Delete the file `supabase/elken-seed.sql` per decision D-08. This SQL file is redundant now that `scripts/seed-elken.mjs` is the canonical seed.

Run: `rm supabase/elken-seed.sql`

Stage the deletion: `git rm supabase/elken-seed.sql`
  </action>
  <verify>
    <automated>test ! -f supabase/elken-seed.sql && echo "PASS: SQL seed deleted" || echo "FAIL: SQL seed still exists"</automated>
  </verify>
  <acceptance_criteria>
    - File `supabase/elken-seed.sql` does not exist on disk
    - `git status` shows the file as deleted/staged
  </acceptance_criteria>
  <done>The redundant SQL seed file is removed from the repository. Only `scripts/seed-elken.mjs` remains as the canonical seed.</done>
</task>

</tasks>

<verification>
- `grep -c "Ask Ethan Digital" scripts/seed-elken.mjs` returns >= 3
- `grep -c "genqigex@gmail.com" scripts/seed-elken.mjs` returns >= 1
- `grep -c "slot_full" scripts/seed-elken.mjs` returns >= 3 (3 languages)
- `grep "ignoreDuplicates: false" scripts/seed-elken.mjs` returns a match
- `grep "booking_enabled" scripts/seed-elken.mjs` returns a match
- `test ! -f supabase/elken-seed.sql` passes
- `npx vitest run` exits 0
</verification>

<success_criteria>
Running `node scripts/seed-elken.mjs` against a Supabase instance will create the complete Elken tenant, "Ask Ethan Digital" bot with personality config (greetings, tone, booking_enabled), all GenQi FAQs in EN/BM/ZH, and all 11 response templates in EN/BM/ZH. The SQL seed file is deleted.
</success_criteria>

<output>
After completion, create `.planning/phases/07-integration-and-launch/07-01-SUMMARY.md`
</output>
