---
phase: 07-integration-and-launch
plan: 03
type: execute
wave: 1
depends_on: ["07-01", "07-02"]
files_modified:
  - scripts/seed-elken.mjs
  - .planning/REQUIREMENTS.md
autonomous: true
requirements: [SEED-02, SEED-03]
gap_closure: true

must_haves:
  truths:
    - "buildFaqs() returns exactly 12 EN, 12 BM, and 12 ZH FAQ objects (36 total)"
    - "REQUIREMENTS.md SEED-03 lists 'no_product_found' instead of 'general_enquiry'"
  artifacts:
    - path: "scripts/seed-elken.mjs"
      provides: "Complete trilingual FAQ seed data"
      contains: "Siapa yang boleh menggunakan bilik mesyuarat"
    - path: ".planning/REQUIREMENTS.md"
      provides: "Accurate requirement text for SEED-03"
      contains: "no_product_found"
  key_links:
    - from: "scripts/seed-elken.mjs"
      to: "faqs table"
      via: "buildFaqs() array insert"
      pattern: "language.*bm.*meeting|language.*zh.*会议室"
---

<objective>
Close two verification gaps from 07-VERIFICATION.md: (1) add 4 missing FAQ translations (2 BM + 2 ZH) to the seed script so all 12 FAQ questions exist in all 3 languages, and (2) update REQUIREMENTS.md SEED-03 text to reflect the documented 'no_product_found' substitution for 'general_enquiry'.

Purpose: Bring Phase 7 verification score from 8/10 to 10/10 and align documentation with implementation.
Output: Updated seed-elken.mjs with 36 FAQ rows, updated REQUIREMENTS.md with correct intent key.
</objective>

<execution_context>
@/Users/abiramivasudevan/.claude/get-shit-done/workflows/execute-plan.md
@/Users/abiramivasudevan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-integration-and-launch/07-CONTEXT.md
@.planning/phases/07-integration-and-launch/07-VERIFICATION.md

@scripts/seed-elken.mjs
@.planning/REQUIREMENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add missing BM and ZH FAQ translations to seed-elken.mjs</name>
  <files>scripts/seed-elken.mjs</files>

  <read_first>
    - scripts/seed-elken.mjs (current state — find the BM FAQ block ending around line 241 and ZH FAQ block ending around line 289)
    - .planning/phases/07-integration-and-launch/07-CONTEXT.md (GenQi FAQ factual content in decisions section)
    - .planning/phases/07-integration-and-launch/07-VERIFICATION.md (gap 1 details)
  </read_first>

  <action>
Add 2 missing BM FAQ objects and 2 missing ZH FAQ objects to the buildFaqs() function in scripts/seed-elken.mjs.

**BM FAQ 1 — OKR Meeting Room members-only policy**
Insert after the existing BM OKR BES FAQ (the one with question "Adakah peranti BES tersedia di GenQi Old Klang Road?") and before the BM Subang section. Add this object:

```javascript
{
  language: 'bm',
  question: 'Siapa yang boleh menggunakan bilik mesyuarat di GenQi Old Klang Road?',
  answer: 'Bilik mesyuarat di GenQi Old Klang Road adalah untuk ahli Elken sahaja. Kad keahlian Elken yang sah diperlukan untuk membuat tempahan. Bilik Mesyuarat Kecil memuatkan sehingga 8 orang; Bilik Mesyuarat Besar memuatkan sehingga 50 orang. Kedua-duanya dilengkapi TV dan projektor.',
},
```

**BM FAQ 2 — Subang BES availability**
Insert after the existing BM Subang facilities FAQ (the one with question "Kemudahan apa yang tersedia di GenQi Subang?") and before the BM general rules FAQ. Add this object:

```javascript
{
  language: 'bm',
  question: 'Adakah peranti BES tersedia di GenQi Subang?',
  answer: 'Ya! Peranti BES tersedia untuk dipinjam bagi sesi katil di GenQi Subang — terdapat 4 unit BES. Sila maklumkan semasa membuat tempahan jika anda ingin menggunakan peranti BES.',
},
```

**ZH FAQ 1 — OKR Meeting Room members-only policy**
Insert after the existing ZH OKR BES FAQ (the one with question "GenQi旧巴生路是否提供BES设备借用？") and before the ZH Subang section. Add this object:

```javascript
{
  language: 'zh',
  question: 'GenQi旧巴生路的会议室谁可以使用？',
  answer: 'GenQi旧巴生路的会议室仅限Elken会员使用，预约时需出示有效的Elken会员证。小型会议室最多容纳8人，大型会议室最多容纳50人，两间会议室均配备电视和投影仪。',
},
```

**ZH FAQ 2 — Subang BES availability**
Insert after the existing ZH Subang facilities FAQ (the one with question "GenQi梳邦提供哪些设施？") and before the ZH general rules FAQ. Add this object:

```javascript
{
  language: 'zh',
  question: 'GenQi梳邦是否提供BES设备借用？',
  answer: '是的！GenQi梳邦提供BES设备借用服务，共有4台可供床位疗程使用。预约时请告知我们是否需要使用BES设备。',
},
```

After insertion, verify the total FAQ count: the buildFaqs() return array should contain exactly 36 objects (12 EN + 12 BM + 12 ZH).
  </action>

  <verify>
    <automated>node -e "const m = require('fs').readFileSync('scripts/seed-elken.mjs','utf8'); const en=(m.match(/language:\s*'en'/g)||[]).length; const bm=(m.match(/language:\s*'bm'/g)||[]).length; const zh=(m.match(/language:\s*'zh'/g)||[]).length; console.log('EN:'+en+' BM:'+bm+' ZH:'+zh+' Total:'+(en+bm+zh)); if(en!==12||bm!==12||zh!==12) process.exit(1); console.log('PASS')"</automated>
  </verify>

  <acceptance_criteria>
    - grep "Siapa yang boleh menggunakan bilik mesyuarat" scripts/seed-elken.mjs returns a match
    - grep "Adakah peranti BES tersedia di GenQi Subang" scripts/seed-elken.mjs returns a match
    - grep "GenQi旧巴生路的会议室谁可以使用" scripts/seed-elken.mjs returns a match
    - grep "GenQi梳邦是否提供BES设备借用" scripts/seed-elken.mjs returns a match
    - FAQ language counts: EN=12, BM=12, ZH=12 (36 total)
    - node --check scripts/seed-elken.mjs exits 0 (no syntax errors)
  </acceptance_criteria>

  <done>buildFaqs() returns 36 FAQ objects: 12 EN, 12 BM, 12 ZH. All GenQi FAQ questions (including OKR meeting room policy and Subang BES availability) have translations in all three languages.</done>
</task>

<task type="auto">
  <name>Task 2: Update REQUIREMENTS.md SEED-03 to reflect no_product_found</name>
  <files>.planning/REQUIREMENTS.md</files>

  <read_first>
    - .planning/REQUIREMENTS.md (find SEED-03 line — currently line 119)
    - .planning/phases/07-integration-and-launch/07-CONTEXT.md (documents the intentional substitution in the specifics section)
  </read_first>

  <action>
In .planning/REQUIREMENTS.md, find the SEED-03 line (line 119):

Current text:
```
- [x] **SEED-03**: All Elken response templates (slot_full, booking_confirmed_member, booking_confirmed_nonmember, reminder_24h, post_survey, general_enquiry) are pre-seeded in EN, BM, and ZH
```

Replace `general_enquiry` with `no_product_found`:

New text:
```
- [x] **SEED-03**: All Elken response templates (slot_full, booking_confirmed_member, booking_confirmed_nonmember, reminder_24h, post_survey, no_product_found) are pre-seeded in EN, BM, and ZH
```

This aligns the requirement text with the actual implementation and the documented decision in 07-CONTEXT.md (the client did not provide a general_enquiry script; no_product_found was the intended template).
  </action>

  <verify>
    <automated>grep "SEED-03" .planning/REQUIREMENTS.md | grep -q "no_product_found" && ! grep "SEED-03" .planning/REQUIREMENTS.md | grep -q "general_enquiry" && echo "PASS" || echo "FAIL"</automated>
  </verify>

  <acceptance_criteria>
    - grep "SEED-03.*no_product_found" .planning/REQUIREMENTS.md returns a match
    - grep "SEED-03.*general_enquiry" .planning/REQUIREMENTS.md returns NO match
    - The rest of the SEED-03 line is unchanged (slot_full, booking_confirmed_member, booking_confirmed_nonmember, reminder_24h, post_survey all still present)
  </acceptance_criteria>

  <done>REQUIREMENTS.md SEED-03 accurately lists the 6 template intent keys matching the seed implementation: slot_full, booking_confirmed_member, booking_confirmed_nonmember, reminder_24h, post_survey, no_product_found.</done>
</task>

</tasks>

<verification>
1. `node --check scripts/seed-elken.mjs` exits 0 (syntax valid)
2. FAQ language count: EN=12, BM=12, ZH=12 (total 36)
3. REQUIREMENTS.md SEED-03 contains "no_product_found" and does NOT contain "general_enquiry"
4. All 4 new FAQ strings are present in seed-elken.mjs (grep for each question text)
</verification>

<success_criteria>
- Phase 07 verification gaps fully closed: both PARTIAL truths from 07-VERIFICATION.md are now VERIFIED
- seed-elken.mjs produces 36 FAQ rows (12 per language) when run
- REQUIREMENTS.md text matches the actual seed implementation for SEED-03
</success_criteria>

<output>
After completion, create `.planning/phases/07-integration-and-launch/07-03-SUMMARY.md`
</output>
