---
phase: 06-analytics
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - tests/api/analytics.test.ts
  - tests/lib/csv.test.ts
autonomous: true
requirements: [ANAL-01, ANAL-02, ANAL-03, ANAL-04, ANAL-05, ANAL-06, ANAL-07, ANAL-08, ANAL-09, ANAL-10, ANAL-11, ANAL-12]

must_haves:
  truths:
    - "tests/api/analytics.test.ts exists with describe blocks for all 11 report types"
    - "tests/lib/csv.test.ts exists with describe blocks for downloadCsv utility"
    - "Both test files follow the same import and describe patterns as existing test files in tests/"
  artifacts:
    - path: "tests/api/analytics.test.ts"
      provides: "API route test stubs for ANAL-01 through ANAL-11"
    - path: "tests/lib/csv.test.ts"
      provides: "Unit test stubs for downloadCsv (ANAL-12)"
  key_links:
    - from: "tests/api/analytics.test.ts"
      to: "app/api/analytics/[botId]/route.ts"
      via: "import or fetch mock"
      pattern: "analytics"
---

<objective>
Wave 0: Create test infrastructure stubs before implementation begins.

Purpose: Establish test files with describe/it blocks so that the Nyquist validation contract is satisfied and tests can be filled in as tasks complete.
Output: Two test files with stubs that fail (no implementation yet) but compile and run with vitest.
</objective>

<execution_context>
@/Users/abiramivasudevan/.claude/get-shit-done/workflows/execute-plan.md
@/Users/abiramivasudevan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-analytics/06-VALIDATION.md
@.planning/phases/06-analytics/06-RESEARCH.md

<interfaces>
Check for existing test files to copy the pattern from:
- tests/api/ directory (look for existing API test files)
- tests/lib/ directory (look for existing lib test files)
- vitest.config.ts (test configuration)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create analytics API route test stubs</name>
  <files>tests/api/analytics.test.ts</files>
  <read_first>
    - vitest.config.ts (test setup, globals, environment)
    - tests/api/ directory listing — find any existing API test file (e.g., tests/api/config.test.ts or tests/api/bookings.test.ts) to copy structure
    - If tests/api/config.test.ts exists, read it for import pattern and describe/it structure
  </read_first>
  <action>
Create `tests/api/analytics.test.ts` with stub describe/it blocks for each of the 11 analytics report types.

Follow the exact import pattern and describe structure from the existing API test file (e.g., tests/api/config.test.ts). If no existing API test file exists, use this pattern:

```typescript
import { describe, it, expect, vi, beforeEach } from 'vitest'

// Stub: implementation not yet built — tests will be filled in after Plan 01 completes

describe('GET /api/analytics/[botId]', () => {
  describe('report=message-volume (ANAL-01)', () => {
    it('returns array of {day, channel, count} for the given date range', async () => {
      // TODO: implement after app/api/analytics/[botId]/route.ts is created
      expect(true).toBe(true) // placeholder — remove when implementing
    })
  })

  describe('report=intent (ANAL-02)', () => {
    it('returns array of {intent, count} for all 5 intents', async () => {
      expect(true).toBe(true)
    })
  })

  describe('report=unanswered (ANAL-03)', () => {
    it('returns rows with {content, frequency} sorted by frequency DESC', async () => {
      expect(true).toBe(true)
    })
  })

  describe('report=latency (ANAL-04)', () => {
    it('returns {p50, p95} numbers from percentile_cont RPC', async () => {
      expect(true).toBe(true)
    })
  })

  describe('report=confirmed (ANAL-05)', () => {
    it('returns bookings with status=confirmed in the date range', async () => {
      expect(true).toBe(true)
    })
    it('filters by location when location param is provided', async () => {
      expect(true).toBe(true)
    })
  })

  describe('report=cancellations (ANAL-06)', () => {
    it('returns bookings with status=cancelled', async () => {
      expect(true).toBe(true)
    })
  })

  describe('report=facility (ANAL-07)', () => {
    it('returns array of {facility_type, count}', async () => {
      expect(true).toBe(true)
    })
  })

  describe('report=location (ANAL-08)', () => {
    it('returns {okr: N, subang: N} counts', async () => {
      expect(true).toBe(true)
    })
  })

  describe('report=audit (ANAL-09)', () => {
    it('returns bookings with non-empty audit_log', async () => {
      expect(true).toBe(true)
    })
  })

  describe('report=survey (ANAL-10)', () => {
    it('returns bookings with non-null survey_response', async () => {
      expect(true).toBe(true)
    })
  })

  describe('report=funnel (ANAL-11)', () => {
    it('returns {enquiries, submitted, confirmed, attended} counts', async () => {
      expect(true).toBe(true)
    })
  })

  describe('error handling', () => {
    it('returns 400 when report param is missing', async () => {
      expect(true).toBe(true)
    })
    it('returns 400 for unknown report name', async () => {
      expect(true).toBe(true)
    })
  })
})
```

If there's an existing test file with a different import style (e.g., using test() instead of it(), or a different setup), match that style exactly.
  </action>
  <verify>
    <automated>npx vitest run tests/api/analytics.test.ts 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - tests/api/analytics.test.ts exists
    - File contains describe block "GET /api/analytics/[botId]"
    - File contains 11 report-specific describe blocks (message-volume, intent, unanswered, latency, confirmed, cancellations, facility, location, audit, survey, funnel)
    - confirmed describe block contains a test for location filtering (ANAL-05)
    - `npx vitest run tests/api/analytics.test.ts` exits 0 (all stubs pass)
  </acceptance_criteria>
  <done>Test stub file created — all 11 report type stubs defined, vitest passes with placeholder assertions</done>
</task>

<task type="auto">
  <name>Task 2: Create CSV utility test stubs</name>
  <files>tests/lib/csv.test.ts</files>
  <read_first>
    - vitest.config.ts (test setup, globals, environment)
    - tests/lib/ directory listing — find any existing lib test file to copy structure
    - tests/api/analytics.test.ts (just created — use same import style)
  </read_first>
  <action>
Create `tests/lib/csv.test.ts` with stubs for `downloadCsv` utility (ANAL-12).

```typescript
import { describe, it, expect, vi } from 'vitest'

// Stub: implementation not yet built — fill in after lib/analytics/csv.ts is created (Plan 01 Task 2)

describe('downloadCsv (ANAL-12)', () => {
  it('generates a CSV string with header row from object keys', async () => {
    expect(true).toBe(true)
  })

  it('escapes commas in cell values with double quotes', async () => {
    expect(true).toBe(true)
  })

  it('escapes double quotes in cell values as ""', async () => {
    expect(true).toBe(true)
  })

  it('serializes object/array values as JSON strings', async () => {
    expect(true).toBe(true)
  })

  it('returns early without download when rows array is empty', async () => {
    expect(true).toBe(true)
  })

  it('triggers a browser anchor click to download the file', async () => {
    expect(true).toBe(true)
  })
})
```

Match the import style from tests/api/analytics.test.ts.
  </action>
  <verify>
    <automated>npx vitest run tests/lib/csv.test.ts 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - tests/lib/csv.test.ts exists
    - File contains describe block "downloadCsv (ANAL-12)"
    - File contains at least 5 it() blocks covering: header row, comma escaping, quote escaping, empty array, browser download
    - `npx vitest run tests/lib/csv.test.ts` exits 0 (all stubs pass)
  </acceptance_criteria>
  <done>CSV test stub created — 6 test cases stubbed, vitest passes</done>
</task>

</tasks>

<verification>
- `npx vitest run tests/api/analytics.test.ts tests/lib/csv.test.ts 2>&1 | tail -5` — both files pass
</verification>

<success_criteria>
- tests/api/analytics.test.ts exists with 11 report describe blocks and location filter test
- tests/lib/csv.test.ts exists with 6 csv utility test stubs
- Both files run with vitest and exit 0
- VALIDATION.md Wave 0 requirements fulfilled
</success_criteria>

<output>
After completion, update `.planning/phases/06-analytics/06-VALIDATION.md` frontmatter: set `wave_0_complete: true`
Then create `.planning/phases/06-analytics/06-00-SUMMARY.md`
</output>
