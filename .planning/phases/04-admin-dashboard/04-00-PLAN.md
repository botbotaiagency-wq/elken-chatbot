---
phase: 04-admin-dashboard
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - tests/api/config.test.ts
  - tests/api/faqs.test.ts
  - tests/api/templates.test.ts
  - tests/api/test-chat.test.ts
autonomous: true
requirements: [CONF-01, CONF-02, CONF-03, CONF-04, CONF-05, TEST-01, TEST-02, TEST-03, TEST-04]

must_haves:
  truths:
    - "All 4 test stub files exist and vitest discovers them"
    - "Each test file has describe blocks matching the API routes it covers"
    - "Running vitest on the stub files exits cleanly with pending/todo status"
  artifacts:
    - path: "tests/api/config.test.ts"
      provides: "Test stubs for personality and guardrails config API routes"
      contains: "describe"
    - path: "tests/api/faqs.test.ts"
      provides: "Test stubs for FAQ CRUD API route"
      contains: "describe"
    - path: "tests/api/templates.test.ts"
      provides: "Test stubs for template CRUD API route"
      contains: "describe"
    - path: "tests/api/test-chat.test.ts"
      provides: "Test stubs for test-chat and debug API routes"
      contains: "describe"
  key_links: []
---

<objective>
Create Wave 0 test stub files for all Phase 4 API routes so that subsequent plans can use vitest as their automated verification command.

Purpose: Nyquist compliance requires every plan to have meaningful automated verification beyond `npx next build`. These stubs define the test structure that executors will fill in as they implement each route.
Output: 4 test files with describe blocks, it.todo stubs, and hoisted mock scaffolds matching the established test pattern from Phase 3.
</objective>

<execution_context>
@/Users/abiramivasudevan/.claude/get-shit-done/workflows/execute-plan.md
@/Users/abiramivasudevan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-admin-dashboard/04-CONTEXT.md
@.planning/phases/04-admin-dashboard/04-RESEARCH.md

<interfaces>
<!-- Existing test pattern from Phase 3 -->
From tests/api/keys.test.ts:
```typescript
import { describe, it, expect, vi, beforeEach } from 'vitest'

const { mockFn } = vi.hoisted(() => {
  return { mockFn: vi.fn() }
})

vi.mock('@/lib/supabase/service', () => ({
  createServiceClient: () => { /* chain builder */ }
}))
```

From tests/setup.ts:
```typescript
process.env.VOYAGE_API_KEY = 'test-voyage-key'
process.env.ANTHROPIC_API_KEY = 'test-anthropic-key'
process.env.NEXT_PUBLIC_SUPABASE_URL = 'http://localhost:54321'
process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY = 'test-anon-key'
process.env.SUPABASE_SERVICE_ROLE_KEY = 'test-service-key'
```

From vitest.config.ts:
```typescript
// globals: true, environment: 'node', setupFiles: ['./tests/setup.ts']
// alias: '@' -> root
```

<!-- API routes these tests will cover (created in Plans 01-04) -->
- app/api/config/[botId]/personality/route.ts — GET + PATCH
- app/api/config/[botId]/guardrails/route.ts — GET + PATCH
- app/api/config/[botId]/faqs/route.ts — GET + POST + PATCH + DELETE
- app/api/config/[botId]/templates/route.ts — GET + PATCH
- app/api/config/[botId]/test-chat/route.ts — POST
- app/api/config/[botId]/debug/route.ts — GET
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create 4 test stub files with describe blocks and it.todo stubs</name>
  <files>
    tests/api/config.test.ts,
    tests/api/faqs.test.ts,
    tests/api/templates.test.ts,
    tests/api/test-chat.test.ts
  </files>
  <read_first>
    tests/api/keys.test.ts,
    tests/setup.ts,
    vitest.config.ts
  </read_first>
  <action>
Create 4 test stub files following the established pattern from `tests/api/keys.test.ts`. Each file should:
- Import `{ describe, it, expect, vi, beforeEach }` from 'vitest'
- Set up hoisted mock factories for `@/lib/supabase/service` (same chain-builder pattern as keys.test.ts)
- Have describe blocks for each API route handler being tested
- Use `it.todo()` for each test case (stubs only — implementation comes in Plans 01-04)

**tests/api/config.test.ts** (covers CONF-01, CONF-02, CONF-03):
```typescript
import { describe, it, expect, vi, beforeEach } from 'vitest'

const { mockSelectSingle, mockUpdate } = vi.hoisted(() => {
  return {
    mockSelectSingle: vi.fn(),
    mockUpdate: vi.fn(),
  }
})

vi.mock('@/lib/supabase/service', () => ({
  createServiceClient: () => {
    const chain: Record<string, unknown> = {}
    chain.eq = vi.fn(() => chain)
    chain.single = vi.fn(async () => mockSelectSingle())
    chain.select = vi.fn(() => chain)
    chain.update = vi.fn(async () => mockUpdate())
    chain.from = vi.fn(() => chain)
    return { from: chain.from }
  },
}))

describe('GET /api/config/[botId]/personality', () => {
  it.todo('returns personality fields for a valid botId')
  it.todo('returns 404 when bot does not exist')
})

describe('PATCH /api/config/[botId]/personality', () => {
  it.todo('updates personality fields and returns ok')
  it.todo('validates tone is one of Professional/Friendly/Formal')
  it.todo('saves fallback_message (CONF-02)')
})

describe('GET /api/config/[botId]/guardrails', () => {
  it.todo('returns guardrails fields for a valid botId')
})

describe('PATCH /api/config/[botId]/guardrails', () => {
  it.todo('updates guardrails fields and returns ok')
  it.todo('validates max_response_length is a positive integer')
})
```

**tests/api/faqs.test.ts** (covers CONF-05):
```typescript
import { describe, it, expect, vi, beforeEach } from 'vitest'

const { mockSelect, mockInsert, mockDelete, mockPatchUpdate } = vi.hoisted(() => {
  return {
    mockSelect: vi.fn(),
    mockInsert: vi.fn(),
    mockDelete: vi.fn(),
    mockPatchUpdate: vi.fn(),
  }
})

vi.mock('@/lib/supabase/service', () => ({
  createServiceClient: () => {
    const chain: Record<string, unknown> = {}
    chain.eq = vi.fn(() => chain)
    chain.order = vi.fn(async () => mockSelect())
    chain.select = vi.fn(() => chain)
    chain.insert = vi.fn(() => chain)
    chain.single = vi.fn(async () => mockInsert())
    chain.delete = vi.fn(() => chain)
    chain.update = vi.fn(() => chain)
    chain.from = vi.fn(() => chain)
    return { from: chain.from }
  },
}))

describe('GET /api/config/[botId]/faqs', () => {
  it.todo('returns all FAQs for the bot')
  it.todo('filters by language query param when provided')
})

describe('POST /api/config/[botId]/faqs', () => {
  it.todo('creates a new FAQ and returns 201')
  it.todo('validates language is en/bm/zh')
  it.todo('validates question and answer are non-empty')
})

describe('PATCH /api/config/[botId]/faqs', () => {
  it.todo('updates an existing FAQ by faqId')
  it.todo('scopes update by bot_id for security')
})

describe('DELETE /api/config/[botId]/faqs', () => {
  it.todo('deletes FAQ by faqId scoped to bot_id')
})
```

**tests/api/templates.test.ts** (covers CONF-04):
```typescript
import { describe, it, expect, vi, beforeEach } from 'vitest'

const { mockSelect, mockUpsert } = vi.hoisted(() => {
  return {
    mockSelect: vi.fn(),
    mockUpsert: vi.fn(),
  }
})

vi.mock('@/lib/supabase/service', () => ({
  createServiceClient: () => {
    const chain: Record<string, unknown> = {}
    chain.eq = vi.fn(() => chain)
    chain.order = vi.fn(async () => mockSelect())
    chain.select = vi.fn(() => chain)
    chain.upsert = vi.fn(async () => mockUpsert())
    chain.from = vi.fn(() => chain)
    return { from: chain.from }
  },
}))

describe('GET /api/config/[botId]/templates', () => {
  it.todo('returns all templates for the bot')
  it.todo('filters by language query param when provided')
})

describe('PATCH /api/config/[botId]/templates', () => {
  it.todo('upserts template with onConflict bot_id,intent_key,language')
  it.todo('validates intent_key is one of the 5 known intents')
  it.todo('validates language is en/bm/zh')
})
```

**tests/api/test-chat.test.ts** (covers TEST-01, TEST-02, TEST-03, TEST-04):
```typescript
import { describe, it, expect, vi, beforeEach } from 'vitest'

const { mockBotSelect, mockConversation, mockLogMessage } = vi.hoisted(() => {
  return {
    mockBotSelect: vi.fn(),
    mockConversation: vi.fn(),
    mockLogMessage: vi.fn(),
  }
})

vi.mock('@/lib/supabase/service', () => ({
  createServiceClient: () => {
    const chain: Record<string, unknown> = {}
    chain.eq = vi.fn(() => chain)
    chain.single = vi.fn(async () => mockBotSelect())
    chain.select = vi.fn(() => chain)
    chain.from = vi.fn(() => chain)
    chain.in = vi.fn(() => chain)
    chain.order = vi.fn(() => chain)
    chain.limit = vi.fn(() => chain)
    return { from: chain.from }
  },
}))

vi.mock('@/lib/rag/detect', () => ({
  detectIntentAndLanguage: vi.fn(async () => ({ intent: 'general', language: 'en' })),
}))

vi.mock('@/lib/rag/retrieve', () => ({
  retrieveContext: vi.fn(async () => ({ chunks: [], found: false })),
}))

vi.mock('@/lib/rag/prompt', () => ({
  buildSystemPrompt: vi.fn(() => 'mock system prompt'),
}))

vi.mock('@/lib/rag/logger', () => ({
  getOrCreateConversation: vi.fn(async () => 'mock-conv-id'),
  logMessage: vi.fn(async () => {}),
}))

describe('POST /api/config/[botId]/test-chat', () => {
  it.todo('accepts a message and returns a streamed response (TEST-01)')
  it.todo('does NOT require API key authentication')
  it.todo('returns X-Intent header (TEST-02)')
  it.todo('returns X-Rag-Found header (TEST-02)')
  it.todo('returns X-Conversation-Id header')
  it.todo('honors language_override in request body (TEST-03)')
  it.todo('generates new conversationId when none provided (TEST-04)')
})

describe('GET /api/config/[botId]/debug', () => {
  it.todo('returns debug metadata for the last assistant message')
  it.todo('returns 400 when conversationId is missing')
  it.todo('returns 404 when conversation does not belong to bot')
  it.todo('resolves source chunks with content preview and document name')
})
```

Each file follows the exact same pattern as `tests/api/keys.test.ts`: hoisted mock factories, `vi.mock` for `@/lib/supabase/service`, describe blocks for each HTTP method handler. The `it.todo()` stubs will show as "pending" in vitest output, not as failures, so plans can run `npx vitest run` without false negatives.
  </action>
  <verify>
    <automated>cd "/Users/abiramivasudevan/Elken Whatspp Chatbot" && npx vitest run tests/api/config.test.ts tests/api/faqs.test.ts tests/api/templates.test.ts tests/api/test-chat.test.ts 2>&1 | tail -20</automated>
  </verify>
  <done>All 4 test stub files exist under tests/api/, vitest discovers them, and all tests show as "todo" (pending) status — no failures. Subsequent plans can reference these files in their verify commands.</done>
</task>

</tasks>

<verification>
1. `npx vitest run tests/api/config.test.ts` exits cleanly with todo tests
2. `npx vitest run tests/api/faqs.test.ts` exits cleanly with todo tests
3. `npx vitest run tests/api/templates.test.ts` exits cleanly with todo tests
4. `npx vitest run tests/api/test-chat.test.ts` exits cleanly with todo tests
5. All 4 files follow the hoisted mock pattern from keys.test.ts
</verification>

<success_criteria>
- 4 test files created: config.test.ts, faqs.test.ts, templates.test.ts, test-chat.test.ts
- Each file has describe blocks for the API routes it covers
- Each file uses it.todo() stubs (not it() with empty bodies)
- vitest discovers and runs all 4 files without errors
- Mock scaffolds match the Phase 3 pattern (hoisted factories + vi.mock)
</success_criteria>

<output>
After completion, create `.planning/phases/04-admin-dashboard/04-00-SUMMARY.md`
</output>
