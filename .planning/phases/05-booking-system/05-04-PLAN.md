---
phase: 05-booking-system
plan: 04
type: execute
wave: 3
depends_on: ["05-01", "05-03"]
files_modified:
  - app/dashboard/bookings/page.tsx
  - app/dashboard/bots/[botId]/booking/page.tsx
  - app/dashboard/bots/[botId]/layout.tsx
autonomous: true
requirements:
  - BADM-01
  - BADM-02
  - BADM-07
  - BOOK-15
  - BOOK-16
must_haves:
  truths:
    - "Staff can view all bookings in a filterable table with location, status, facility type, and date range filters"
    - "Booking status badges display correct colors: amber=pending, green=confirmed, red=cancelled, gray=no-show, blue=walk-in"
    - "Staff can confirm, cancel, or mark no-show with inline confirmation rows"
    - "Staff can register a walk-in via the Dialog modal"
    - "Staff can view booking details and audit trail in a Sheet side panel"
    - "Staff can edit booking fields (date, time, facility, notes) from the Sheet"
    - "Facility configuration page allows admin to set capacity, duration, and cutoff rules per facility type"
  artifacts:
    - path: "app/dashboard/bookings/page.tsx"
      provides: "Bookings management page with filterable table, walk-in dialog, detail sheet"
      min_lines: 300
    - path: "app/dashboard/bots/[botId]/booking/page.tsx"
      provides: "Facility configuration page for booking settings"
      min_lines: 100
    - path: "app/dashboard/bots/[botId]/layout.tsx"
      provides: "Updated bot detail layout with Booking tab"
      contains: "booking"
  key_links:
    - from: "app/dashboard/bookings/page.tsx"
      to: "/api/bookings/[botId]"
      via: "fetch calls for CRUD operations"
      pattern: "fetch.*api/bookings"
    - from: "app/dashboard/bots/[botId]/booking/page.tsx"
      to: "facilities_config table"
      via: "API calls to manage facility config"
      pattern: "facilities_config"
---

<objective>
Build the staff-facing bookings management page at /dashboard/bookings and the facility configuration page at /dashboard/bots/[botId]/booking. The bookings page displays a filterable table with inline status actions, walk-in registration dialog, and booking detail sheet with audit trail.

Purpose: Staff need to manage all GenQi facility bookings — view, filter, confirm, cancel, mark no-show, register walk-ins, and view audit history. Admin needs to configure facility capacity and booking rules.
Output: Full bookings management page, facility config page, updated bot layout with new tab.
</objective>

<execution_context>
@/Users/abiramivasudevan/.claude/get-shit-done/workflows/execute-plan.md
@/Users/abiramivasudevan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-booking-system/05-CONTEXT.md
@.planning/phases/05-booking-system/05-UI-SPEC.md
@.planning/phases/05-booking-system/05-01-SUMMARY.md
@.planning/phases/05-booking-system/05-03-SUMMARY.md

<interfaces>
From lib/booking/types.ts:
```typescript
export type BookingStatus = 'pending' | 'confirmed' | 'cancelled' | 'no_show' | 'walk_in'
export type FacilityType = 'bed_female' | 'bed_male' | 'bed_unisex' | 'inhaler' | 'room_small' | 'room_large'
export type BookingLocation = 'okr' | 'subang'
export interface Booking { id, bot_id, facility_type, location, session_start, session_end, customer_name, member_id, contact_number, is_member, has_bes_device, on_loan_unit, customer_gender, status, user_id, channel, conversation_id, audit_log, reminder_sent, reminder_sent_at, survey_sent, survey_sent_at, survey_response, created_at, updated_at }
export interface AuditLogEntry { action, by, at, note }
export interface FacilityConfig { id, bot_id, facility_type, capacity, duration_minutes, min_advance_hours, max_window_days, created_at }
export const FACILITY_LABELS: Record<FacilityType, string>
export const LOCATION_LABELS: Record<BookingLocation, string>
```

From app/api/bookings/[botId]/route.ts (Plan 03):
- GET: query params location, status, facility_type, date_from, date_to → returns Booking[]
- POST: body { customer_name, contact_number, facility_type, location, session_start, ... } → creates walk-in
- PATCH: body { bookingId, action, staffName, note, ... } → status change or edit

Existing dashboard pattern from app/dashboard/bots/[botId]/api-keys/page.tsx:
- 'use client' + useParams() + useState
- Card + CardHeader + CardContent layout
- Dialog for modals
- toast() from sonner for confirmations
- Inline confirm rows for destructive actions (bg-destructive/5)
- formatRelativeTime() and formatDate() utility functions

Existing bot layout from app/dashboard/bots/[botId]/layout.tsx:
- usePathname + pathname.startsWith(href) for active tab
- Tab links as pathname-based navigation
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build bookings management page</name>
  <files>app/dashboard/bookings/page.tsx</files>
  <read_first>
    app/dashboard/bookings/page.tsx
    app/dashboard/bots/[botId]/api-keys/page.tsx
    app/dashboard/bots/[botId]/faqs/page.tsx
    components/ui/sheet.tsx
    components/ui/select.tsx
    components/ui/dialog.tsx
    components/ui/badge.tsx
    .planning/phases/05-booking-system/05-UI-SPEC.md
    .planning/phases/05-booking-system/05-CONTEXT.md
    lib/booking/types.ts
  </read_first>
  <action>
Replace the placeholder at `app/dashboard/bookings/page.tsx` with the full bookings management page. Follow the UI-SPEC exactly.

**Page structure:**
```
'use client'
```

The page needs a bot selector first because bookings are per-bot. Add a bot dropdown at the top. Fetch bots list from `/api/bots` on mount. Once a bot is selected, fetch bookings from `/api/bookings/[botId]`.

**State variables:**
- `bots`: array of `{ id, name }` — fetched from `/api/bots`
- `selectedBotId`: string | null
- `bookings`: Booking[] — fetched from `/api/bookings/[selectedBotId]`
- `loading`: boolean
- `locationFilter`: 'all' | 'okr' | 'subang'
- `statusFilter`: 'all' | BookingStatus
- `facilityFilter`: 'all' | FacilityType
- `dateFrom`: string (ISO date) | ''
- `dateTo`: string (ISO date) | ''
- `confirmingId`: string | null — ID of booking in inline confirm mode
- `confirmAction`: 'confirm' | 'cancel' | 'no_show' | null
- `walkInOpen`: boolean — Dialog open state
- `sheetOpen`: boolean — Sheet open state
- `selectedBooking`: Booking | null — for Sheet detail view
- `editMode`: boolean — Sheet edit mode toggle
- `actionLoading`: boolean — disable buttons during API calls

**Layout (following UI-SPEC page layout structure):**

1. **Page header:** CalendarDays icon from lucide + `h1.text-2xl.font-semibold` "Bookings" + `p.text-muted-foreground` description.

2. **Bot selector:** If no bot selected, show a Select dropdown to pick a bot. Once selected, show the bookings card.

3. **Card with CardHeader:**
   - CardTitle "All Bookings"
   - Filter bar: `div.flex.gap-2.flex-wrap` containing:
     - Select for Location: All / GenQi Old Klang Road / GenQi Subang (values: 'all', 'okr', 'subang')
     - Select for Status: All / Pending / Confirmed / Cancelled / No-show / Walk-in (values: 'all', 'pending', 'confirmed', 'cancelled', 'no_show', 'walk_in')
     - Select for Facility: All + all 6 facility types using FACILITY_LABELS
     - Date inputs: two `Input type="date"` for From and To
   - Button "Register Walk-in" (primary variant) on the right, opens Dialog

4. **CardContent with table:**
   - `<table className="w-full">` with `<thead>` and `<tbody>`
   - Columns: Customer, Facility, Location, Date/Time, Status, Actions
   - Customer column: `customer_name` + `contact_number` below in `text-xs text-muted-foreground`
   - Facility column: use `FACILITY_LABELS[booking.facility_type]`
   - Location column: use `LOCATION_LABELS[booking.location]`
   - Date/Time: format `session_start` with `toLocaleDateString` + `toLocaleTimeString`
   - Status: Badge component with variant="outline" and color overrides per status:
     - pending: `className="border-amber-300 text-amber-700 bg-amber-50"`
     - confirmed: `className="border-green-300 text-green-700 bg-green-50"`
     - cancelled: `className="border-red-300 text-red-700 bg-red-50"`
     - no_show: `className="border-gray-300 text-gray-500 bg-gray-50"`
     - walk_in: `className="border-blue-300 text-blue-700 bg-blue-50"`
   - Actions column:
     - If status === 'pending': Button "Confirm" (size="sm" variant="outline"), Button "Cancel" (size="sm" variant="ghost" className="text-destructive")
     - If status === 'confirmed': Button "No-show" (size="sm" variant="ghost" className="text-destructive")
     - All statuses: Button "View" (size="sm" variant="ghost") with Eye icon → opens Sheet
   - When `confirmingId === booking.id`: show inline confirm row (`tr className="bg-destructive/5"` with `td colSpan={6}`) with:
     - Confirm action: "Confirm this booking? A confirmation message will be sent to the customer." — "Confirm" / "Keep pending"
     - Cancel action: "Cancel this booking? This cannot be undone." — "Yes, cancel" / "Keep booking"
     - No-show action: "Mark this booking as no-show? The customer will not receive a survey." — "Mark no-show" / "Keep confirmed"

5. **Empty state:** When no bookings and not loading: `div.py-12.text-center` with heading "No bookings yet" and body "Bookings submitted through the bot will appear here. You can also register a walk-in customer using the button above."

6. **Loading state:** `div.py-8.text-center.text-sm.text-muted-foreground` "Loading bookings..."

**Walk-in Dialog:**
- `Dialog` + `DialogContent className="sm:max-w-lg"`
- `DialogHeader` with title "Register Walk-in Customer" and description "Walk-in bookings are confirmed immediately — no staff approval required."
- Form fields:
  - Input "Full Name" (required)
  - Input "Member ID" (optional)
  - Input "Contact Number" (required)
  - Select "Member Status": Member / Non-member
  - Select "Facility Type": all 6 types from FACILITY_LABELS
  - Select "Location": filtered by facility type using FACILITY_LOCATION_CONSTRAINTS (auto-select if only one option)
  - Input type="datetime-local" "Date & Time" (required)
  - Textarea "Notes" (optional)
- Button "Register Walk-in" (primary, full-width) — calls POST `/api/bookings/[botId]`
- Button "Go Back" (outline, full-width) — closes dialog
- On success: toast "Walk-in registered successfully.", close dialog, refresh bookings list.

**Booking Detail Sheet:**
- `Sheet` + `SheetContent side="right"`
- `SheetHeader` with title "Booking Details"
- Booking fields in a grid (label/value pairs):
  - Customer Name, Member ID, Contact Number, Facility, Location, Date/Time, Status (badge), Member, BES Device
- Edit mode toggle: Button "Edit" → switches values to Input/Select fields. Show "Save Changes" + "Cancel Edit" buttons.
  - On save: call PATCH `/api/bookings/[botId]` with action='edit' and changed fields.
  - On success: toast "Booking updated.", refresh bookings, exit edit mode.
- Separator
- "Audit Trail" section header (`text-base font-semibold`)
- Scrollable `div.max-h-64.overflow-y-auto` with audit log entries:
  - Each entry: `div` with timestamp (`text-xs text-muted-foreground`), action text, staff name, note
  - Format timestamp with `formatRelativeTime` or `toLocaleString`
- Notification status row: show `reminder_sent` and `survey_sent` status with badges: "Sent" (green), "Pending" (default), "Failed" (red) based on retry count >= 3.

**Fetch logic:**
- On bot select change: `fetch('/api/bookings/${selectedBotId}?${filterParams}')` with all active filters as query params.
- Refresh after any mutation (confirm, cancel, no-show, walk-in, edit).
- Filters trigger a new fetch with updated query params.

**Action handlers:**
- `handleConfirm(bookingId)`: PATCH with `{ bookingId, action: 'confirm', staffName: 'Admin', note: '' }`. On success: toast "Booking confirmed. Customer will be notified.", refresh.
- `handleCancel(bookingId)`: PATCH with `{ bookingId, action: 'cancel', staffName: 'Admin', note: '' }`. On success: toast "Booking cancelled.", refresh.
- `handleNoShow(bookingId)`: PATCH with `{ bookingId, action: 'no_show', staffName: 'Admin', note: '' }`. On success: toast "Marked as no-show.", refresh.
- Error handling: try/catch with toast "Something went wrong. Please try again." on failure.

Use `Toaster` from `@/components/ui/sonner` — add `<Toaster />` at the end of the component JSX.
  </action>
  <verify>
    <automated>cd "/Users/abiramivasudevan/Elken Whatspp Chatbot" && grep "'use client'" app/dashboard/bookings/page.tsx && grep "CalendarDays" app/dashboard/bookings/page.tsx && grep "Register Walk-in" app/dashboard/bookings/page.tsx && grep "border-amber-300" app/dashboard/bookings/page.tsx && grep "border-green-300" app/dashboard/bookings/page.tsx && grep "border-red-300" app/dashboard/bookings/page.tsx && grep "border-blue-300" app/dashboard/bookings/page.tsx && grep "Sheet" app/dashboard/bookings/page.tsx && grep "Dialog" app/dashboard/bookings/page.tsx && grep "Audit Trail" app/dashboard/bookings/page.tsx && grep "No bookings yet" app/dashboard/bookings/page.tsx && echo "ALL CHECKS PASSED"</automated>
  </verify>
  <acceptance_criteria>
    - app/dashboard/bookings/page.tsx starts with 'use client'
    - Page contains CalendarDays icon and h1 "Bookings"
    - Filter bar contains Select components for Location, Status, Facility type
    - Filter bar contains date range inputs
    - "Register Walk-in" primary button opens Dialog
    - Table columns: Customer, Facility, Location, Date/Time, Status, Actions
    - Status badges use correct color classes: amber for pending, green for confirmed, red for cancelled, gray for no-show, blue for walk-in
    - Inline confirm rows appear with bg-destructive/5 class
    - Confirm action copy: "Confirm this booking? A confirmation message will be sent to the customer."
    - Cancel action copy: "Cancel this booking? This cannot be undone."
    - No-show action copy: "Mark this booking as no-show? The customer will not receive a survey."
    - Walk-in Dialog has title "Register Walk-in Customer" and description "Walk-in bookings are confirmed immediately"
    - Walk-in form contains fields: Name, Member ID, Contact, Member Status, Facility, Location, DateTime, Notes
    - Walk-in submit button text: "Register Walk-in"
    - Walk-in cancel button text: "Go Back"
    - Sheet displays "Booking Details" title
    - Sheet contains "Audit Trail" section
    - Sheet has edit mode with "Save Changes" button
    - Empty state shows "No bookings yet" heading
    - Loading state shows "Loading bookings..."
    - Toast messages match UI-SPEC copywriting: "Walk-in registered successfully.", "Booking confirmed. Customer will be notified.", "Booking cancelled.", "Marked as no-show.", "Booking updated."
    - Error toast: "Something went wrong. Please try again."
    - Notification status shows reminder_sent and survey_sent badges
  </acceptance_criteria>
  <done>Bookings management page fully implemented with filterable table, 5 status badge colors, inline confirm/cancel/no-show actions, walk-in registration dialog, booking detail sheet with audit trail, edit mode, and notification status indicators. All copy matches UI-SPEC.</done>
</task>

<task type="auto">
  <name>Task 2: Build facility config page and add Booking tab to bot layout</name>
  <files>app/dashboard/bots/[botId]/booking/page.tsx, app/dashboard/bots/[botId]/layout.tsx</files>
  <read_first>
    app/dashboard/bots/[botId]/layout.tsx
    app/dashboard/bots/[botId]/personality/page.tsx
    app/dashboard/bots/[botId]/guardrails/page.tsx
    lib/booking/types.ts
    .planning/phases/05-booking-system/05-UI-SPEC.md
    .planning/phases/05-booking-system/05-CONTEXT.md
  </read_first>
  <action>
**1. Create `app/dashboard/bots/[botId]/booking/page.tsx`:**

A facility configuration page where admin configures booking parameters per facility type, booking feature toggle, and n8n outbound webhook URL.

```
'use client'
```

**State:**
- `configs`: FacilityConfig[] — fetched from API
- `webhookUrl`: string — the bot's n8n_outbound_webhook value
- `bookingEnabled`: boolean — the bot's feature_flags.booking_enabled value
- `loading`: boolean
- `saving`: boolean

**Layout:**
- Page header: `h1.text-2xl.font-semibold` "Booking Configuration"
- `p.text-muted-foreground` "Configure facility types, booking rules, and notification settings for this bot."

**Card 1: Booking Module Toggle**
- CardTitle "Booking Module"
- A labeled toggle/checkbox: "Enable booking module for this bot" — controls `feature_flags.booking_enabled`
- Save button that PATCHes the bot's feature_flags via `/api/bots/[botId]/config` (use the existing config API pattern from Phase 4)

**Card 2: Facility Configuration**
- CardTitle "Facility Types"
- Table with columns: Facility Type, Capacity, Duration (min), Min Advance (hours), Max Window (days)
- Each row is one of the 6 facility types (use FACILITY_LABELS for display)
- Each cell is an `Input type="number"` with the current value
- If no config exists for a facility type, show defaults (capacity=1, duration=60, min_advance=2, max_window=30)
- Button "Save Booking Config" at the bottom
- On save: POST or PUT to a new facility config API endpoint `/api/bots/[botId]/facilities` that upserts all 6 rows into `facilities_config` table. Use `createServiceClient()` on the server side.
  - For simplicity, create the API route inline OR use an existing pattern. The save handler should call `fetch('/api/bots/${botId}/facilities', { method: 'POST', body: JSON.stringify({ configs }) })`.
  - The API route at `app/api/bots/[botId]/facilities/route.ts` should upsert (INSERT ON CONFLICT UPDATE) each facility config row.

**Card 3: n8n Outbound Webhook**
- CardTitle "Notification Webhook"
- Input labeled "n8n Outbound Webhook URL" with value `webhookUrl`
- Helper text: "The URL n8n will receive booking notifications on. Required for confirmation, reminders, and survey messages."
- Save button that PATCHes `bots.n8n_outbound_webhook` via a direct API call

**Fetch on mount:**
- GET `/api/bots/${botId}/facilities` to load facility configs
- GET `/api/bots/${botId}` or use existing bot data to get `n8n_outbound_webhook` and `feature_flags.booking_enabled`

**Also create `app/api/bots/[botId]/facilities/route.ts`:**
- GET: `SELECT * FROM facilities_config WHERE bot_id = botId` using service client
- POST: For each of the 6 facility types in the request body, upsert into facilities_config using `INSERT INTO ... ON CONFLICT (bot_id, facility_type) DO UPDATE SET capacity = ..., duration_minutes = ..., min_advance_hours = ..., max_window_days = ...`. Use service client.

**2. Update `app/dashboard/bots/[botId]/layout.tsx`:**

Add a new tab link for "Booking" to the existing bot detail navigation. Read the current layout to find the tabs array/list. Add:
```typescript
{ href: `/dashboard/bots/${botId}/booking`, label: 'Booking' }
```
Place it after the existing tabs (Personality, Guardrails, FAQs, Templates, Testing, API Keys, Integrations) or after Testing.

Follow the existing pattern: `usePathname` + `pathname.startsWith(href)` for active state detection.
  </action>
  <verify>
    <automated>cd "/Users/abiramivasudevan/Elken Whatspp Chatbot" && test -f app/dashboard/bots/\[botId\]/booking/page.tsx && grep "Booking Configuration" app/dashboard/bots/\[botId\]/booking/page.tsx && grep "n8n_outbound_webhook\|n8n Outbound Webhook\|Notification Webhook" app/dashboard/bots/\[botId\]/booking/page.tsx && grep "booking_enabled\|Booking Module" app/dashboard/bots/\[botId\]/booking/page.tsx && grep "booking" app/dashboard/bots/\[botId\]/layout.tsx && echo "ALL CHECKS PASSED"</automated>
  </verify>
  <acceptance_criteria>
    - app/dashboard/bots/[botId]/booking/page.tsx exists and starts with 'use client'
    - Page contains "Booking Configuration" heading
    - Page has a "Booking Module" card with enable/disable toggle for booking_enabled feature flag
    - Page has a "Facility Types" card with a table of 6 facility types
    - Table columns: Facility Type, Capacity, Duration (min), Min Advance (hours), Max Window (days)
    - Each value cell is an Input type="number"
    - Page has "Save Booking Config" button
    - Page has "Notification Webhook" card with Input for n8n outbound webhook URL
    - Webhook helper text: "The URL n8n will receive booking notifications on."
    - app/api/bots/[botId]/facilities/route.ts exists with GET and POST handlers
    - app/dashboard/bots/[botId]/layout.tsx contains "booking" tab link
  </acceptance_criteria>
  <done>Facility configuration page allows admin to toggle booking module, configure capacity/duration/cutoff per facility type, and set n8n outbound webhook URL. Bot detail layout includes new "Booking" tab.</done>
</task>

</tasks>

<verification>
- Bookings management page displays filterable table with correct status badges
- Walk-in registration creates bookings via Dialog modal
- Booking detail Sheet shows fields + audit trail + notification status
- Facility config page allows per-facility-type configuration
- Bot layout has Booking tab
</verification>

<success_criteria>
- Staff can view, filter, and manage bookings from /dashboard/bookings
- Staff can register walk-ins via Dialog
- Staff can confirm/cancel/mark-no-show with inline confirm rows
- Staff can view audit trail in Sheet side panel
- Staff can edit booking fields from Sheet
- Admin can configure facility capacity and booking rules
- Admin can set n8n outbound webhook URL
</success_criteria>

<output>
After completion, create `.planning/phases/05-booking-system/05-04-SUMMARY.md`
</output>
